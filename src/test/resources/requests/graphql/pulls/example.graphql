{
  repository(name: "Hello-World", owner: "octocat") {
    pullRequests(last: 100) {
      totalCount
      nodes {
        url, createdAt, mergedAt, closedAt
      }
      pageInfo { startCursor, hasPreviousPage }
    }
  }
}